// PDF.js Worker - Local fallback
// This is a minimal worker implementation for PDF.js
self.onmessage = function(e) {
  // Basic worker functionality
  if (e.data.type === 'test') {
    self.postMessage({ type: 'ready' });
  }
};